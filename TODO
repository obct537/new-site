setup mod_rewrite

create a more official "view" system

Fix all the god-awful forms

Re-orginize the site (separate core and modules)

create a password reset system [needs more testing]

finish/un-fuck the user activation system [needs more testing]

Create a class-based model system [in progress]

make password page re-direct to previous page

possibly move functions/model.php into the the model class?

create a routing system?

system to handle GET variables a bit smoother

change password error-checking to JavaScript

input validation system

come up with some sort of rules about naming conventions (it's gotten that bad)
